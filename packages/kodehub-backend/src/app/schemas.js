import sharedSchema from '../shared/schemas';
import userSchema from '../user/schema';
import bookmarkSchema from '../bookmark/schema';

const schemas = [sharedSchema, userSchema, bookmarkSchema];

export default schemas;
